# 贡献指南

感谢您对 Photo Truck 的关注！我们欢迎各种形式的贡献。

## 🛠️ 开发环境准备

### 前置要求

- **Node.js**: 18.0+
- **Rust**: 1.70+
- **ExifTool**: 用于读取照片元数据

### 安装步骤

```bash
# 克隆项目
git clone https://github.com/ZhangHuan95/photo-truck.git
cd photo-truck

# 安装 Node.js 依赖
npm install

# 安装 Rust (如果未安装)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 安装 ExifTool
brew install exiftool  # macOS
# 或
sudo apt install libimage-exiftool-perl  # Ubuntu/Debian
```

## 🚀 开发命令

### 开发模式

```bash
# 启动开发服务器（带热重载）
npm run tauri dev
```

### 构建生产版本

```bash
# 构建当前平台
npm run tauri build

# 输出位置
# macOS: src-tauri/target/release/bundle/dmg/
# Windows: src-tauri/target/release/bundle/msi/
# Linux: src-tauri/target/release/bundle/appimage/
```

### 运行测试

```bash
cd src-tauri
cargo test
```

## 📁 项目结构

```
photo-truck/
├── src/                          # Vue 前端代码
│   ├── App.vue                   # 主应用组件
│   ├── main.ts                   # 应用入口
│   └── styles.css                # 样式文件
├── src-tauri/                    # Rust 后端代码
│   ├── src/
│   │   ├── lib.rs               # 库入口，模块注册
│   │   ├── main.rs              # 应用入口
│   │   ├── classify.rs          # 分类逻辑和模板处理
│   │   ├── commands.rs          # Tauri 命令处理器
│   │   ├── exif.rs              # ExifTool 集成
│   │   ├── hash.rs              # SHA-256 哈希和去重
│   │   ├── transfer.rs          # 文件传输逻辑
│   │   ├── transfer_v2.rs       # 支持取消的传输引擎
│   │   ├── history.rs           # 传输历史记录
│   │   ├── rename.rs            # 批量重命名
│   │   ├── thumbnail.rs         # 缩略图提取
│   │   └── cli.rs               # 命令行模式
│   ├── tests/
│   │   └── integration_test.rs  # 集成测试
│   ├── Cargo.toml               # Rust 依赖配置
│   └── tauri.conf.json          # Tauri 应用配置
├── public/                       # 静态资源
├── package.json                  # Node.js 依赖配置
├── README.md                     # 项目介绍
└── CONTRIBUTING.md              # 贡献指南（本文件）
```

## 🧪 测试覆盖

| 模块 | 测试数量 | 描述 |
|------|----------|------|
| classify.rs | 24 | 日期解析、文件夹命名、分类模板、扩展名支持 |
| hash.rs | 17 | SHA-256 哈希、快速哈希、去重器 |
| exif.rs | 10 | EXIF 元数据、ExifTool 检查 |
| transfer.rs | 28 | 文件扫描、进度追踪、大小格式化 |
| history.rs | 7 | 历史记录增删改查 |
| rename.rs | 6 | 重命名模板变量替换 |
| thumbnail.rs | 4 | 缩略图提取 |
| cli.rs | 3 | 命令行参数解析 |
| 集成测试 | 16 | 端到端工作流、序列化、并发安全 |
| **总计** | **112+** | 全部通过 ✅ |

## 📝 提交规范

我们使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <description>

[可选的正文]

[可选的脚注]
```

### 类型 (type)

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具变动

### 示例

```
feat(transfer): 添加传输取消功能

- 使用 AtomicBool 实现取消标志
- 每传输一个文件检查取消状态
- 添加相关测试用例

Closes #123
```

## 🔀 Pull Request 流程

1. **Fork 本仓库**

2. **创建特性分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **进行开发**
   - 编写代码
   - 添加测试
   - 确保所有测试通过

4. **提交更改**
   ```bash
   git add .
   git commit -m "feat: your feature description"
   ```

5. **推送分支**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 填写 PR 模板
   - 等待代码审查

## 🐛 报告问题

在提交 Issue 之前，请：

1. 搜索现有 Issues，避免重复
2. 使用清晰的标题描述问题
3. 提供以下信息：
   - 操作系统和版本
   - Photo Truck 版本
   - 复现步骤
   - 期望行为和实际行为
   - 相关日志或截图

## 💡 功能建议

欢迎提出新功能建议！请在 Issue 中描述：

- 您想要的功能
- 为什么需要这个功能
- 可能的实现方式（可选）

## 📋 代码风格

### Rust

- 使用 `cargo fmt` 格式化代码
- 使用 `cargo clippy` 检查代码质量
- 遵循 Rust 官方命名规范

### TypeScript/Vue

- 使用 2 空格缩进
- 组件使用 PascalCase 命名
- 变量使用 camelCase 命名

## 📄 许可证

通过贡献代码，您同意您的贡献将以 MIT 许可证发布。

---

如有任何问题，请随时在 Issues 中提问或联系维护者。

感谢您的贡献！🙏
